include $(top_srcdir)/Makefile.pkg

SUBDIRS =

my_INSTDIR = $(my_PKGDATADIR)/WSP/PDU
my_INSTFILES += ConfirmedPush.pm Disconnect.pm Push.pm Suspend.pm

my_EXTRA_DIST += $(my_INSTFILES) ConfirmedPush.t Disconnect.t Push.t Suspend.t

